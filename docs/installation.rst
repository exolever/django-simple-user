============
Installation
============

At the command line::

    $ easy_install simple_user

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv simple_user
    $ pip install simple_user
